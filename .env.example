# Tuya cloud credentials (one-time setup to get local keys)
TUYA_API_REGION=us
TUYA_API_KEY=
TUYA_API_SECRET=
TUYA_API_DEVICE_ID=

# Philips Hue bridge IP (auto-discovered if blank)
HUE_BRIDGE_IP=

# Network to scan (CIDR notation, auto-detected if blank)
SCAN_NETWORK=
SCAN_TIMEOUT=5
